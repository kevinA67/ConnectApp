export type User = {
    email: string;
    name: string;
    password: string;
}

export type Contacts = {
    uid: string;
    email: string;
    displayName: string;
}


export type Messages = {
    emisor: string;
    mensaje: string;
    fecha: string;
}

